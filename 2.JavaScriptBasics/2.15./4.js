const pow = () => {
  let a = prompt("Your first number?");
  let b = prompt("Your second number?");

  let result = a;

  for (let i = 1; i < n; i++) {
    result *= a;
  }
  //return Math.pow(a, b)

  return result;
};
